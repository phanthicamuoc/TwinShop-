% extends 'base.html.twig' %}

{% block title %}Customer detail{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h1>Customer detail</h1>
        </div>
        <div class="card-body">
            <p class="card-text"><span class="fs-6 fw-bold">Phone ID: </span>{{ product.id }}</p>
            <p class="card-text"><span class="fs-6 fw-bold">Product Name: </span>{{ product.proName }}</p>
        </div>
    </div>


{% endblock %}